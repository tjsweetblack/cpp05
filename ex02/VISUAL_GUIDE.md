# 📊 Visual Guide: Ex02 - Abstract Classes and Polymorphism

## Class Hierarchy Diagram

```
                        ┌─────────────────────────┐
                        │     AForm (Abstract)    │
                        ├─────────────────────────┤
                        │ - name: const string    │
                        │ - is_signed: bool       │
                        │ - grade_to_sign: const  │
                        │ - grade_to_execute:const│
                        ├─────────────────────────┤
                        │ + beSigned()            │
                        │ # checkExecution()      │
                        │ + execute() = 0 (pure) │◄────┐
                        │ + virtual ~AForm()      │     │ Inheritance
                        └────────────┬────────────┘     │
                                     │                  │
                    ┌────────────────┼────────────────┐ │
                    │                │                │ │
       ┌────────────▼────────┐  ┌───▼──────────────┐ │┌▼────────────────────┐
       │ ShrubberyCreation   │  │  RobotomyRequest │ ││ PresidentialPardon  │
       │      Form           │  │      Form        │ ││       Form          │
       ├─────────────────────┤  ├──────────────────┤ │├─────────────────────┤
       │ - target: string    │  │ - target: string │ ││ - target: string    │
       ├─────────────────────┤  ├──────────────────┤ │├─────────────────────┤
       │ Sign: 145           │  │ Sign: 72         │ ││ Sign: 25            │
       │ Exec: 137           │  │ Exec: 45         │ ││ Exec: 5             │
       ├─────────────────────┤  ├──────────────────┤ │├─────────────────────┤
       │ + execute()         │  │ + execute()      │ ││ + execute()         │
       │   Creates file with │  │   Makes drilling │ ││   Prints pardon     │
       │   ASCII trees       │  │   noises, 50%    │ ││   message           │
       │                     │  │   success rate   │ ││                     │
       └─────────────────────┘  └──────────────────┘ │└─────────────────────┘
```

## Polymorphism in Action

```
Base Class Pointer → Different Object Types

    AForm *form;
       │
       ├─────> ShrubberyCreationForm("home")
       │       │
       │       └─> execute() → Creates home_shrubbery
       │
       ├─────> RobotomyRequestForm("target")
       │       │
       │       └─> execute() → Makes noises, 50% success
       │
       └─────> PresidentialPardonForm("criminal")
               │
               └─> execute() → Prints pardon message

Same pointer type, different behaviors!
```

## Virtual Function Call Flow

```
                     Client Code
                         │
                         ▼
              AForm *form = new ShrubberyCreationForm("home");
                         │
                         ▼
                  form->execute(bureaucrat);
                         │
                         ├──> Compiler Step:
                         │    "form is AForm*, check if execute() is virtual"
                         │    "Yes, use vtable lookup"
                         │
                         ├──> Runtime Step:
                         │    1. Dereference vptr in object
                         │    2. Look up execute() in vtable
                         │    3. Find ShrubberyCreationForm::execute()
                         │
                         ▼
        ShrubberyCreationForm::execute(Bureaucrat const &executor)
                         │
                         ├──> checkExecution(executor)
                         │    (calls base class protected method)
                         │
                         └──> Create file and write ASCII trees


If execute() was NOT virtual:
    → Would call AForm::execute() (pointer type)
    → Wrong behavior!
```

## Memory Layout with Vtable

```
Object in Memory: ShrubberyCreationForm

┌────────────────────────────────────┐
│ vptr (8 bytes)                     │──────┐
├────────────────────────────────────┤      │
│ name: "ShrubberyCreationForm"     │      │
├────────────────────────────────────┤      │
│ is_signed: false                   │      │
├────────────────────────────────────┤      │
│ grade_to_sign: 145                 │      │
├────────────────────────────────────┤      │
│ grade_to_execute: 137              │      │
├────────────────────────────────────┤      │
│ target: "home"                     │      │
└────────────────────────────────────┘      │
                                            │
                                            ▼
                    Vtable for ShrubberyCreationForm
                    ┌─────────────────────────────────────┐
                    │ execute() ────────────────────────┐ │
                    ├───────────────────────────────────┼─┤
                    │ ~ShrubberyCreationForm() ────────┐│ │
                    ├──────────────────────────────────┼┼─┤
                    │ ~AForm() ────────────────────────┼┼─┤
                    └──────────────────────────────────┴┴─┘
                            │                        │
                            ▼                        ▼
           ShrubberyCreationForm    ShrubberyCreationForm
                ::execute()           ::~ShrubberyCreationForm()
```

## Complete Execution Flow

```
Bureaucrat boss("Boss", 1);
AForm *form = new ShrubberyCreationForm("garden");
boss.signForm(*form);
boss.executeForm(*form);
delete form;

┌─────────────────────────────────────────────────────────────┐
│ 1. Create Bureaucrat                                        │
│    Boss: [name="Boss", grade=1]                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Create Form (Polymorphic)                                │
│    Memory: ShrubberyCreationForm object                     │
│    Pointer: AForm*                                          │
│    ├─> AForm constructor: grades set                        │
│    └─> ShrubberyCreationForm: target="garden"              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Sign Form                                                │
│    boss.signForm(*form)                                     │
│    └─> form.beSigned(boss)                                  │
│        ├─> Check: boss.grade (1) <= grade_to_sign (145) ✓  │
│        └─> is_signed = true                                 │
│    Print: "Boss signed ShrubberyCreationForm"              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Execute Form (POLYMORPHISM HERE!)                        │
│    boss.executeForm(*form)                                  │
│    └─> form.execute(boss)  ← Virtual call!                 │
│        │                                                     │
│        ├─> Vtable lookup                                    │
│        │   Finds: ShrubberyCreationForm::execute()          │
│        │                                                     │
│        └─> ShrubberyCreationForm::execute(boss)             │
│            ├─> checkExecution(boss)                         │
│            │   ├─> Check: is_signed? true ✓                 │
│            │   └─> Check: boss.grade (1) <= exec (137) ✓    │
│            │                                                 │
│            └─> Create "garden_shrubbery" file               │
│                Write ASCII trees                            │
│    Print: "Boss executed ShrubberyCreationForm"            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Delete Form (Virtual Destructor!)                        │
│    delete form;                                             │
│    ├─> Vtable lookup for destructor                         │
│    ├─> ShrubberyCreationForm::~ShrubberyCreationForm()     │
│    └─> AForm::~AForm()                                      │
│    Memory freed properly!                                   │
└─────────────────────────────────────────────────────────────┘
```

## Constructor Call Chain

```
Creating: ShrubberyCreationForm("home")

                        START
                          │
                          ▼
        ┌────────────────────────────────┐
        │ Allocate Memory                │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ Call AForm Constructor         │
        │ AForm("ShrubberyCreationForm", │
        │       145, 137)                │
        ├────────────────────────────────┤
        │ - Set name                     │
        │ - Set is_signed = false        │
        │ - Set grade_to_sign = 145      │
        │ - Set grade_to_execute = 137   │
        │ - Validate grades ✓            │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ Initialize Derived Members     │
        │ target = "home"                │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ ShrubberyCreationForm          │
        │ Constructor Body               │
        │ (empty in our case)            │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ Set vptr to point to           │
        │ ShrubberyCreationForm vtable   │
        └────────┬───────────────────────┘
                 │
                 ▼
                READY
```

## Destructor Call Chain

```
Deleting: AForm *form = new ShrubberyCreationForm("home");
         delete form;

              START (delete form)
                  │
                  ▼
        ┌─────────────────────┐
        │ Virtual destructor? │
        │ YES!                │
        └─────────┬───────────┘
                  │
                  ├──> Look up in vtable
                  │
                  ▼
    ┌──────────────────────────────┐
    │ ShrubberyCreationForm        │
    │   ::~ShrubberyCreationForm() │
    ├──────────────────────────────┤
    │ - Print destructor message   │
    │ - Clean up derived members   │
    └──────────┬───────────────────┘
               │
               ▼
    ┌──────────────────────────────┐
    │ AForm::~AForm()              │
    ├──────────────────────────────┤
    │ - Print destructor message   │
    │ - Clean up base members      │
    └──────────┬───────────────────┘
               │
               ▼
    ┌──────────────────────────────┐
    │ Deallocate Memory            │
    └──────────────────────────────┘
               │
               ▼
              END


If destructor was NOT virtual:
    → Only AForm::~AForm() called
    → ShrubberyCreationForm destructor skipped
    → Potential memory leak!
```

## Pure Virtual Function Concept

```
Abstract Class (AForm)
┌─────────────────────────────────────┐
│ virtual void execute(...) = 0       │ ← Pure virtual (= 0)
│                                     │
│ Meaning:                            │
│ ✗ No implementation in AForm        │
│ ✗ Cannot create AForm objects       │
│ ✓ Derived classes MUST implement   │
└─────────────────────────────────────┘
              │
              ├──────────────────────────────┐
              │                              │
              ▼                              ▼
┌─────────────────────────┐    ┌─────────────────────────┐
│ ShrubberyCreationForm   │    │ Missing Implementation  │
├─────────────────────────┤    ├─────────────────────────┤
│ virtual void execute()  │    │ // No execute() here    │
│ {                       │    │                         │
│   // Implementation     │    │ ERROR: Still abstract!  │
│ }                       │    │ Can't instantiate!      │
│                         │    │                         │
│ ✓ Can instantiate       │    │ ✗ Can't instantiate     │
└─────────────────────────┘    └─────────────────────────┘
```

## Protected Members Access

```
┌─────────────────────────────────────┐
│         AForm Class                 │
├─────────────────────────────────────┤
│ private:                            │
│   - name, is_signed, grades         │
│   └─> Only AForm can access         │
│                                     │
│ protected:                          │
│   # checkExecution(...)             │
│   └─> AForm and derived classes     │
│                                     │
│ public:                             │
│   + beSigned(...)                   │
│   + execute(...) = 0                │
│   └─> Everyone can access           │
└─────────────────────────────────────┘
            │
            │ Inheritance
            ▼
┌─────────────────────────────────────┐
│   ShrubberyCreationForm             │
├─────────────────────────────────────┤
│ private:                            │
│   - target                          │
│                                     │
│ public:                             │
│   + execute(...) {                  │
│       checkExecution(...);  ✓       │
│       // Can call protected method! │
│   }                                 │
└─────────────────────────────────────┘


External Code:
┌─────────────────────────────────────┐
│ form.checkExecution(...)  ✗         │
│ // ERROR: protected member          │
│                                     │
│ form.execute(...)  ✓                │
│ // OK: public member                │
└─────────────────────────────────────┘
```

## Grade Requirements Visualization

```
Grade Scale: 1 (Best) ──────────────────────► 150 (Worst)

ShrubberyCreationForm:
    Sign:    ├──────────────────────────────────────┤ 145
    Execute: ├────────────────────────────────┤ 137
    
RobotomyRequestForm:
    Sign:    ├─────────────────┤ 72
    Execute: ├──────────┤ 45
    
PresidentialPardonForm:
    Sign:    ├────┤ 25
    Execute: ├┤ 5

Legend:
├─────┤ = Bureaucrat grade must be ≤ this value

Example:
Grade 50 bureaucrat can:
    ✓ Sign and execute ShrubberyCreationForm (50 ≤ 145, 50 ≤ 137)
    ✓ Sign and execute RobotomyRequestForm (50 ≤ 72, but 50 > 45)
        → Can sign but NOT execute
    ✗ Sign PresidentialPardonForm (50 > 25)
```

## Form Execution Requirements Flowchart

```
                    execute() called
                          │
                          ▼
                ┌─────────────────┐
                │ checkExecution()│
                └────────┬────────┘
                         │
           ┌─────────────┴─────────────┐
           ▼                           ▼
    ┌──────────────┐           ┌──────────────┐
    │  Is signed?  │           │ Grade check  │
    └──────┬───────┘           └──────┬───────┘
           │                          │
       ┌───┴───┐                  ┌───┴───┐
       │       │                  │       │
      YES     NO                 OK      TOO LOW
       │       │                  │       │
       │       └──> Throw         │       └──> Throw
       │           FormNot        │            GradeToo
       │           Signed         │            Low
       │           Exception      │            Exception
       │                          │
       └──────────────┬───────────┘
                      │
                      ▼
            All checks passed!
                      │
                      ▼
        Execute form-specific action
                      │
         ┌────────────┼────────────┐
         │            │            │
         ▼            ▼            ▼
    Create file   Make noises   Print pardon
```

## Polymorphic Container Example

```
Creating an array of different forms:

    AForm *forms[3];
       │
       ├──> [0] ───> ShrubberyCreationForm("home")
       │               │
       │               └─> target: "home"
       │                   grades: 145/137
       │
       ├──> [1] ───> RobotomyRequestForm("Bob")
       │               │
       │               └─> target: "Bob"
       │                   grades: 72/45
       │
       └──> [2] ───> PresidentialPardonForm("Alice")
                       │
                       └─> target: "Alice"
                           grades: 25/5

Processing loop:
    for (int i = 0; i < 3; i++) {
        bureaucrat.signForm(*forms[i]);     ← Same code
        bureaucrat.executeForm(*forms[i]);  ← Same code
        delete forms[i];                    ← Same code
    }
    
    But each iteration executes DIFFERENT behavior!
    ↓
    Iteration 0: Creates file
    Iteration 1: Makes drilling noises
    Iteration 2: Prints pardon message
```

## Object Slicing Warning

```
❌ WRONG - Object Slicing:

void processForm(AForm form) {  // Pass by VALUE
    form.execute(bureaucrat);
}

ShrubberyCreationForm shrub("test");
processForm(shrub);
    │
    └──> Only AForm part copied!
         ┌────────────────┐
         │ vptr (AForm)   │ ← Lost derived vptr!
         │ name           │
         │ is_signed      │
         │ grades         │
         └────────────────┘
         (target lost!)
         
         execute() might not work correctly!


✓ CORRECT - No Slicing:

void processForm(AForm &form) {  // Pass by REFERENCE
    form.execute(bureaucrat);
}

OR

void processForm(AForm *form) {  // Pass by POINTER
    form->execute(bureaucrat);
}

ShrubberyCreationForm shrub("test");
processForm(shrub);  // or processForm(&shrub);
    │
    └──> Reference/pointer to full object
         ┌────────────────────────────┐
         │ vptr (ShrubberyCreation)   │ ← Keeps derived vptr!
         │ name                       │
         │ is_signed                  │
         │ grades                     │
         │ target                     │ ← All data intact!
         └────────────────────────────┘
         
         Polymorphism works correctly!
```

## Quick Reference: Virtual Keywords

```
┌────────────────────────────────────────────────────────┐
│ Keyword         │ Meaning                              │
├─────────────────┼──────────────────────────────────────┤
│ virtual         │ Function can be overridden           │
│                 │ Enable dynamic dispatch              │
├─────────────────┼──────────────────────────────────────┤
│ = 0             │ Pure virtual (no implementation)     │
│                 │ Makes class abstract                 │
├─────────────────┼──────────────────────────────────────┤
│ virtual ~Class()│ Virtual destructor                   │
│                 │ Essential for proper cleanup         │
├─────────────────┼──────────────────────────────────────┤
│ override (C++11)│ Explicitly mark as override          │
│                 │ Compiler checks correctness          │
└────────────────────────────────────────────────────────┘
```

## Summary: When to Use What

```
┌─────────────────────────────────────────────────────────┐
│ Use Abstract Class When:                                │
├─────────────────────────────────────────────────────────┤
│ ✓ You want to define a common interface                │
│ ✓ You want to prevent direct instantiation             │
│ ✓ You have multiple implementations                    │
│ ✓ You need polymorphism                                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Use Virtual Functions When:                             │
├─────────────────────────────────────────────────────────┤
│ ✓ Derived classes should override behavior             │
│ ✓ You need runtime polymorphism                        │
│ ✓ Working with base class pointers/references          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Use Protected Members When:                             │
├─────────────────────────────────────────────────────────┤
│ ✓ Derived classes need access                          │
│ ✓ External code should NOT access                      │
│ ✓ You want to provide utilities to subclasses          │
└─────────────────────────────────────────────────────────┘
```

---

**Pro Tip:** Draw these diagrams yourself when studying! The act of drawing reinforces understanding of complex relationships.
